// var express = require('express');
// var bodyParser = require('body-parser');
// var app = express();
// var additionPost = require('./routes/selectionsRouter');


function numberCruncher({total_object: inputObjects}) {
   {total_object: req.body.inputObjects}
    if ({type: "add"}) {
        Number({x: ""}) + Number({y: ""});
    } else if ({type: "subtract"}) {
        Number({x: ""}) - Number({y: ""});
    } else if ({type: "multiply"}) {
        Number({x: ""}) * Number({y: ""});
    } else if ({type: "divide"}) {
        Number({x: ""}) / Number({y: ""})
    }
    return totalNumber
}

module.exports = numberCruncher;

//{total_object: inputObjects},